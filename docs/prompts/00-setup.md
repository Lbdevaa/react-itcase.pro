# Промты: подготовка

Claude Code (Opus 5), 2026-07-29.

---

> Изучи README. Есть дополнительное требование заказчика: приложить промты, оставить в репозитории
> спецификацию и правила, сгенерированный код зафиксировать первоначальным коммитом, а все последующие
> изменения — отдельными, чтобы было видно, что именно делал разработчик. Насколько это выполнимо и что
> для этого потребуется?

> Оформи план отдельным md-файлом — будем двигаться по нему поэтапно и фиксировать коммитами.
> Нужен пример проекта на FSD?

> FSD можно взять в урезанном виде. Допиши сравнение с Atomic Design.
> Посмотри проект […], там есть рабочая реализация FSD.

Уточняющие вопросы по стеку: выбраны TypeScript, миграция на Vite и React 19.

> Спецификация в порядке, зафиксировал коммитом. Идём дальше — на каждом шаге подсказывай, как коммитить.

---

Итог: созданы [SPEC.md](../SPEC.md) и [CLAUDE.md](../../CLAUDE.md). Архитектура — FSD в усечённом виде;
микрофронтенды и Atomic Design рассмотрены и отклонены (обоснование в спецификации).
